// Este archivo ya no es necesario ya que la lógica fue movida al contexto
// Se mantiene para compatibilidad con versiones anteriores y evitar errores

export const useImageConverter = () => {
  console.warn(
    "DEPRECATED: useImageConverter hook está obsoleto. Usa useImageConverterContext en su lugar.",
  );
  return {};
};
